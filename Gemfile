source 'https://rubygems.org'

gem 'rake'
gem 'rake_cloudspin', :path => '../rake_cloudspin'
gem 'aws_ssh_key', :path => '../aws_ssh_key'
gem 'aws_encrypted_bucket', :path => '../aws_encrypted_bucket'
